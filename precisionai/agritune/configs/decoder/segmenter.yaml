# Segmenter-style mask transformer: per-class embeddings jointly processed with patch tokens
# through shared self-attention layers, then a scaled dot product yields per-pixel mask logits.
name: segmenter
hidden_dim: 192
num_layers: 2
num_heads: 3
mlp_ratio: 4
dropout: 0.0
